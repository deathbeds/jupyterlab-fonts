on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ${{ matrix.os }}-latest
    strategy:
      matrix:
        os: ['ubuntu']
        python-version: ['3.9']
    steps:
      - name: checkout
        uses: actions/checkout@v2
